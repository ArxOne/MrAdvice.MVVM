<Window x:Class="TestApplication.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mrAdvice="urn:MrAdvice/View" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:testApplication="clr-namespace:TestApplication"
        mc:Ignorable="d" Visibility="Visible"
        Title="MainView" Height="350" Width="525" ShowInTaskbar="True" d:DataContext="{d:DesignInstance testApplication:MainViewModel}">
    <Grid Margin="8">
        <StackPanel Orientation="Vertical">
            <Button Content="Click me" Command="{mrAdvice:Command {Binding ButtonAction}}" HorizontalAlignment="Left" />
            <TextBlock>
                <Run Text="You clicked me "/>
                <Run Text="{Binding ButtonActionCount, Mode=OneWay}"/>
                <Run Text=" time(s)"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
